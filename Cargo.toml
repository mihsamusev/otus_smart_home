[package]
name = "smart_home"
version = "0.4.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
thiserror = "*"
chrono = "0.4.*"

[dev-dependencies]
# test only deps
float-cmp = "*"